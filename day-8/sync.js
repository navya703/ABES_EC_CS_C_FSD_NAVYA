 console.log(strat);
const waitForSomeTime = (ms) => {
  const startTime = Date.noe;
  console / log(startTime);
  while (Date.now() - start < ms) {
    //do nothing
  }
};
const syncUsingTimer = () => {
  waitForSomeTime(4000);
  console.log("wait has been completed");
};
console.log(sysncFun());
console.log(end);